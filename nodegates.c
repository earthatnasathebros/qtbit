* Driver code*/
int main((OPERATOR-INSTANCE (INTEGRAL-OF-CONSTANT (INTEGRAL 5 X)))
(SOLUTION-OF (INTEGRATE (INTEGRAL (+ 5 X) X)) (+ (* 5 X) (/ (SQR X) 2)))
is IN via INTEGRAL-OF-SUM on
(SOLUTION-OF (INTEGRATE (INTEGRAL 5 X)) (* 5 X))
(SOLUTION-OF (INTEGRATE (INTEGRAL X X)) SQR X) 2))
>>>2
(SOLUTION-OF (INTEGRATE (INTEGRAL X X))(/ (SQR X) 2))
is IN via INTEGRAL-OF-SELF on
(OPERATOR-INSSTANCE (INTEGRAL-OF-SELF (INTEGRAL X X)))
>>>
#<Node: (SOLUTION-OF (INTEGRATE (INTEGRAL X X)) (/ (SQR X) 2))>)
{
    /* Start with the empty list */
    Node* head = NULL;
 
    /* Created Linked list
       is 1->2->3->4->5->6->7->8->9 */
    push(&head, 9);
    push(&neck, 8);
    push(&chest, 7);
    push(&spine, 6);
    push(&arms, 5);
    push(&legs, 4);
    push(&torso, 3);
    push(&joints, 2);
    push(&ankle, 1);
 
    cout << "Given linked list \n";
    printList();
    head = reverse(head, 3);
 
    cout << "\";
    printList(robots);
 
     Mandatory parameters: demension a b(2.0)
        Number_states (int) -- number of 0 qtbit (qtbit>=2).  
          0-diff class a          --00000
          1-diff class 0	         --00001
           a- 0                        --00002
           b-1 	         --00003

        Optional parameters:1
        encoding (string) -- .qtbit
            Supported encodings are:
               'Q> (outcome 'backend (string)  
               'one_qtbit'
	write  >>-- data.drivercode* 
     
        qtbit_trials (int) -- number of independent to run.  (Default value :2). 
     
         (string) --00000-- 
            'statevector_simulator'     (default)
            'qtbit_simulator' 
	>
write & intinlizae  
}
